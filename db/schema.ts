import {sqliteTable,text,integer,index,uniqueIndex} from 'drizzle-orm/sqlite-core';
export const sessions=sqliteTable('sessions',{id:text('id').primaryKey(),ownerId:text('owner_id').notNull(),title:text('title').notNull(),question:text('question').notNull(),status:text('status').notNull().default('draft'),createdAt:text('created_at').notNull()},t=>[index('idx_sessions_owner_created').on(t.ownerId,t.createdAt)]);
export const options=sqliteTable('options',{id:text('id').primaryKey(),sessionId:text('session_id').notNull().references(()=>sessions.id),label:text('label').notNull(),position:integer('position').notNull()},t=>[index('idx_options_session').on(t.sessionId)]);
export const votes=sqliteTable('votes',{id:text('id').primaryKey(),sessionId:text('session_id').notNull().references(()=>sessions.id),optionId:text('option_id').notNull().references(()=>options.id),voterId:text('voter_id').notNull(),createdAt:text('created_at').notNull()},t=>[uniqueIndex('idx_votes_session_voter').on(t.sessionId,t.voterId),index('idx_votes_option').on(t.optionId)]);
