import { sqliteTable, text, integer, index, uniqueIndex } from 'drizzle-orm/sqlite-core';
export var sessions = sqliteTable('sessions', { id: text('id').primaryKey(), ownerId: text('owner_id').notNull(), title: text('title').notNull(), question: text('question').notNull(), status: text('status').notNull().default('draft'), createdAt: text('created_at').notNull() }, function (t) { return [index('idx_sessions_owner_created').on(t.ownerId, t.createdAt)]; });
export var options = sqliteTable('options', { id: text('id').primaryKey(), sessionId: text('session_id').notNull().references(function () { return sessions.id; }), label: text('label').notNull(), position: integer('position').notNull() }, function (t) { return [index('idx_options_session').on(t.sessionId)]; });
export var votes = sqliteTable('votes', { id: text('id').primaryKey(), sessionId: text('session_id').notNull().references(function () { return sessions.id; }), optionId: text('option_id').notNull().references(function () { return options.id; }), voterId: text('voter_id').notNull(), createdAt: text('created_at').notNull() }, function (t) { return [uniqueIndex('idx_votes_session_voter').on(t.sessionId, t.voterId), index('idx_votes_option').on(t.optionId)]; });
